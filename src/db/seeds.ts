// Seeding is now handled by the backend on startup (server/db.ts seedDb())
// This file is kept for compatibility but is a no-op
export async function seedDatabase() {
  // Backend seeds automatically on first run
}
