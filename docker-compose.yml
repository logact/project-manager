services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: project-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/var/lib/project-manager/data
    environment:
      - NODE_ENV=production
      - PORT=3000
